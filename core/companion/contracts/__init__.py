"""Byte-identical copies of cross-plugin contract files from memory_companion 1.10.5.

These files must stay verbatim copies: the companion plugin fingerprint-checks
them against its own copies at handshake time. Never edit in place — re-copy
from the paired release instead. See docs/companion-fork-spec.md section 4.
"""
